<h1 class="title">Comment</h1>

<table class="index">
  <thead>
    <tr>
      <th>name</th>
      <th>comment</th>
    <tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.name %></td>
        <td><%= comment.cmt %></td>
      </tr>
    <% end %>
  </tbody>
</table>



<%= form_for(@comment, url: {:controller => :comments, :action => :index}) do |f| %>

  <div class="field" style="width:200px;">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field" style="width:350px;">
    <%= f.label :cmt %>
    <%= f.text_area :cmt, cols: "30", rows: "10" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
